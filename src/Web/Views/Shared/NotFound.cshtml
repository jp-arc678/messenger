@{
    ViewBag.Title = "ไม่พบหน้าที่เรียก";
}

<div class="row justify-content-center">
    <div class="col-12 col-lg-7">
        <div class="card shadow-sm">
            <div class="card-body text-center py-5">
                <div class="display-6 mb-3">ไม่พบหน้าที่เรียก</div>
                <p class="text-body-secondary">
                    ที่อยู่ที่เรียกไม่มีอยู่ในระบบ หรือข้อมูลที่ต้องการอยู่นอกสาขาของคุณ
                </p>
                <div class="d-flex flex-wrap gap-2 justify-content-center">
                    <a class="btn btn-primary" href="@Url.Action("Index", "Home")">กลับหน้าแรก</a>
                    <a class="btn btn-outline-secondary" href="@Url.Action("Index", "Requests")">ไปหน้าใบแจ้งงาน</a>
                </div>
            </div>
        </div>
    </div>
</div>
