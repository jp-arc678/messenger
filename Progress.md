# Progress — สถานะงาน ณ 18/08/2026 (สิ้นวัน)

> เอกสารส่งต่อสำหรับทำงานต่อวันถัดไป · อ่านไฟล์นี้ก่อนเริ่ม แล้วค่อยดู [CLAUDE.md](CLAUDE.md)
> งานวันนี้ : ปิดของค้างจากขั้นที่ 1 ทั้งหมด แล้วปิดเพิ่มอีก 4 เรื่อง —
> **UAT-01 · 2.4 · 4.5 · 8.1–8.3** · UAT ขยับจาก 54/61 เป็น **59/61** และ**ไม่เหลือข้อบกพร่องเปิดค้าง**

---

## 1. เริ่มพรุ่งนี้ตรงนี้

**ลำดับงานพรุ่งนี้ (ไล่จากบนลงล่าง)**

| # | งาน | สถานะ |
|---|---|---|
| 1 | **8.4 + 8.5 บนมือถือจริง** → ปิด UAT ได้ทันที | ⏳ รอมือถือ 1 เครื่องต่อ Wi-Fi วงเดียวกัน |
| 2 | push commit ขึ้น GitHub | ✅ **เสร็จแล้ว 18/08/2026** (§4.2) |
| 3 | เก็บสคริปต์ทดสอบ 5 ตัวเข้า `tools\` พร้อมให้ล้างข้อมูลตัวเอง | ✅ **เสร็จแล้ว 19/08/2026** (§4.1) |
| 4 | ล้างข้อมูลทดสอบเก่าใน DB | ✅ **ตัดสินใจแล้ว 19/08/2026 : ยังไม่ล้าง** (§4.3) |

> เหลืองานเดียวที่ขวางการปิด UAT คือข้อ 1 — **ทุกอย่างที่ทดสอบแทนได้ ทดสอบครบหมดแล้ว**

### ✅ เสร็จแล้ว (18/08/2026) — ขั้นที่ 1 และ 2 ของแผนเดิม

| งาน | ผล |
|---|---|
| build ทั้ง solution | ✅ ผ่านครบ 5 โปรเจกต์ |
| `dotnet test` | ✅ **257 / 257 ผ่าน** (Failed 0 · 3 วินาที) |
| UAT-01 — ไฟล์เกิน 8 MB ต้องได้ 413 | ✅ พิสูจน์แล้วด้วย `tools\Test-UploadTooLarge.ps1` (8 assertion ผ่านหมด) |
| UAT 2.4 — เส้นแบ่ง 10:00 ของ BR-1 | ✅ พิสูจน์แล้วด้วย `tools\Test-Br1Clock.ps1` (5 assertion ผ่านหมด) |
| UAT 4.5 — ย่อรูปฝั่ง client ≤ 2 MB | ✅ พิสูจน์แล้วด้วย `tools\Test-PhotoResize.ps1` (15 assertion ผ่านหมด) |
| UAT 8.1–8.3 — หน้าจอ 390px | ✅ `tools\Test-MobileLayout.ps1` (64 assertion ผ่านหมด) — **device emulation** ไม่ใช่เครื่องจริง |

**UAT-01 ปิดแล้ว** — ทดสอบซ้ำครบ 4 เคสเดิม : PDF 12 MB → **413** · PNG 10.57 MB → **413**
(ทั้งคู่มีหน้าอธิบายภาษาไทย) · PNG 2.64 MB → "ไฟล์ใหญ่เกิน 2 MB" · PDF 1 MB → "รองรับเฉพาะ JPG และ PNG"
สองเคสหลังยืนยันว่าเส้นทางปกติไม่ถดถอย

**UAT 2.4 ผ่านแล้ว** — ตั้ง `ClockOffsetMinutes` = `-765` (ระบบมองว่า 09:15) → วันที่ส่ง = **วันนี้** ·
`-675` (ระบบมองว่า 10:45) → วันที่ส่ง = **วันทำการถัดไป** · สคริปต์คืนค่า Web.config
เป็นค่าว่างให้เองใน `finally` ทุกกรณี

**UAT 4.5 ผ่านแล้ว** — ไฟล์ต้นทาง **6.58 MB (4032×3024 เท่ารูปจากกล้องมือถือ) → ส่งจริง 223 KB**
วัดจาก Content-Length ที่ Chrome ส่งออกไปเอง ไม่ใช่คำบอกเล่าของหน้าเว็บ · รูปที่เก็บบน server ก็ ≤ 2 MB
· เคสคุม : ไฟล์ 176 KB ส่งของเดิมไม่แปลงซ้ำ · PDF ถูกปฏิเสธตั้งแต่ก่อนส่ง

**UAT 8.1–8.3 ผ่านแล้วด้วย device emulation** (Chrome 390×844 + touch emulation) ไล่ 7 หน้า × 3 role
· ทุกหน้าไม่ต้องเลื่อนซ้าย-ขวา · ตารางทั้ง 4 ตัวอยู่ใน `.table-responsive` · เมนู hamburger
กางครบตามสิทธิ์ (Messenger/Admin 4 รายการ · User 3 รายการ) · ปุ่มในคิวงาน 72 ปุ่มไม่ทับกันเลย
· **ข้อสังเกต** : ปุ่มเลื่อนลำดับ ↑ ↓ กว้าง 25px (สูง 44px) แคบกว่า 44×44 ที่ Apple/Google แนะนำ
— ยังผ่านขั้นต่ำ 24px ของ WCAG จึงไม่นับว่าไม่ผ่าน แต่ถ้าผู้ใช้จริงบ่นว่ากดพลาด ค่อยขยาย

### เหลือทำ — 8.4 กับ 8.5 เท่านั้น ต้องใช้มือถือจริง

- **8.4** แตะช่องเลือกไฟล์รูปแล้วต้องเปิด**กล้องหลัง**ได้ตรง ๆ
- **8.5** ช่องวันที่ต้องเปิด**ปฏิทินของเครื่อง** (จุดเสี่ยงของ D34) · ติ๊กประเภทงานแล้วช่องรายละเอียดโผล่

รัน `tools\Start-ForMobile.ps1` (ครั้งแรกต้องเปิด PowerShell แบบ Run as administrator)
แล้วเปิดจากมือถือที่ `http://192.168.1.39:52080` (Wi-Fi) หรือ `http://10.1.101.51:52080` (LAN)

> ทั้งสองข้อเป็นพฤติกรรมของ **OS มือถือ** ไม่ใช่ของ layout engine จำลองแทนไม่ได้จริง ๆ
> ถ้าเจ้าของระบบอยากได้หลักฐานจากเครื่องจริงของ 8.1–8.3 ด้วย ให้ไล่ซ้ำตอนได้เครื่องมาพร้อมกัน

### วิธีรันเว็บ + ทดสอบซ้ำทั้งชุด

```powershell
# 1. เว็บ (ถ้ามีตัวเก่าค้างอยู่ ต้อง Stop-Process ก่อน ไม่งั้นพอร์ตชนกัน)
& "C:\Program Files\IIS Express\iisexpress.exe" /path:"D:\Projects\Messenger\src\Web" /port:52080

# 2. unit test (257 ตัว)
dotnet test tests\UnitTests\Messenger.UnitTests.csproj
#    ⚠ หลังรัน dotnet test แล้ว ถ้าจะ build ใหม่ ต้อง msbuild -t:restore คั่นก่อน (ดู §6 ข้อ 5)

# 3. เทสต์ที่ยิงของจริงใส่เว็บ — ต้องเปิดเว็บไว้ก่อน
pwsh -File tools\Test-BranchIsolation.ps1     # BR-6 + สิทธิ์ (D33)
pwsh -File tools\Test-UploadTooLarge.ps1      # UAT-01 — ไฟล์เกิน 8 MB ต้องได้ 413
pwsh -File tools\Test-Br1Clock.ps1            # UAT 2.4 — เส้นแบ่ง 10:00 ของ BR-1
pwsh -File tools\Test-PhotoResize.ps1         # UAT 4.5 — ย่อรูปฝั่ง client (ต้องมี Chrome + Node 22+)
pwsh -File tools\Test-MobileLayout.ps1        # UAT 8.1–8.3 — จอ 390px (ต้องมี Chrome + Node 22+)

# 4. เทสต์รายหมวดของเช็คลิสต์ UAT
pwsh -File tools\Test-Section1.ps1            # หมวด 1 เข้าสู่ระบบ + สิทธิ์
pwsh -File tools\Test-Section2.ps1            # หมวด 2 แจ้งงาน
pwsh -File tools\Test-Section3.ps1            # หมวด 3 คิวงาน + state machine
pwsh -File tools\Test-Section4.ps1            # หมวด 4 รูป + BR-4
pwsh -File tools\Test-Section6.ps1            # หมวด 6 รายงาน + export
```

| สคริปต์ใน `tools\` | ครอบคลุม | assertion |
|---|---|---|
| `Test-BranchIsolation.ps1` | BR-6 + สิทธิ์ตาม role (D33) | 32 |
| `Test-UploadTooLarge.ps1` | UAT-01 (413) | 8 |
| `Test-Br1Clock.ps1` | UAT 2.4 (BR-1) | 5 |
| `Test-PhotoResize.ps1` + `photo-resize-probe.mjs` | UAT 4.5 (BR-3) | 15 |
| `Test-MobileLayout.ps1` + `mobile-layout-probe.mjs` | UAT 8.1–8.3 | 64 |
| `Test-Section1.ps1` | หมวด 1 เข้าสู่ระบบ + สิทธิ์ | 21 |
| `Test-Section2.ps1` | หมวด 2 แจ้งงาน | 24 |
| `Test-Section3.ps1` | หมวด 3 คิวงาน + state machine | 35 |
| `Test-Section4.ps1` | หมวด 4 รูป + BR-4 | 26 |
| `Test-Section6.ps1` | หมวด 6 รายงาน + export | 27 |
| `TestSupport.ps1` | ฟังก์ชันร่วม (login · assert · ล้างข้อมูล) | — |

> **รวม 257 assertion** ที่ยิงของจริงใส่เว็บ (นอกเหนือจาก unit test อีก 257 ตัว)
> · `Test-Section*.ps1` **ล้างใบงานที่ตัวเองสร้างให้เองตอนจบ** — ยกเว้นตอนมีข้อไม่ผ่าน
> จะเก็บข้อมูลไว้ให้เปิดดูสาเหตุ (สั่ง `-KeepData` เพื่อเก็บไว้เสมอก็ได้)
> · ส่วน `Test-BranchIsolation.ps1` / `Test-UploadTooLarge.ps1` / `Test-Br1Clock.ps1` /
> `Test-PhotoResize.ps1` / `Test-MobileLayout.ps1` ยังทิ้งใบงานไว้เหมือนเดิม

---

## 2. โค้ดที่แก้ไป — commit แล้วที่ `4dac8d1` (build ผ่าน · **ทดสอบพฤติกรรมแล้ว 18/08/2026**)

5 ไฟล์ (แก้ 4 + ใหม่ 1) แก้ 2 เรื่องพร้อมกัน

### 2.1 แก้ UAT-01 (ไฟล์ใหญ่เกิน 8 MB ได้ HTTP 500 เปล่า ๆ)

| ไฟล์ | ทำอะไร |
|---|---|
| `src/Web/Global.asax.cs` | เพิ่ม `Application_Error` ดักเฉพาะ `WebEventCodes.RuntimeErrorPostTooLarge` → ตอบ **413** พร้อมหน้าอธิบายภาษาไทย · ใช้ `CompleteRequest()` ไม่ใช่ `Response.End()` เพื่อไม่ให้เกิด `ThreadAbortException` ใน log |
| `src/Web/Scripts/photo-upload.js` | เพิ่ม `assertLooksLikeImage()` ปฏิเสธไฟล์ที่ไม่ใช่ JPG/PNG **ตั้งแต่ก่อนส่ง** + ดักสถานะ 413 ให้ขึ้นข้อความที่อ่านรู้เรื่อง |

**ไม่ได้ขยาย `maxRequestLength`** เพราะนั่นแค่ย้ายเส้นแบ่งไปตัวเลขใหม่
ปัญหาจริงคือผู้ใช้ไม่รู้ว่าเกิดอะไรขึ้น

### 2.2 นาฬิกาจำลองสำหรับทดสอบ BR-1

| ไฟล์ | ทำอะไร |
|---|---|
| `src/Infrastructure/Data/OffsetClock.cs` | **ไฟล์ใหม่** — `IClock` ที่เลื่อนเวลาจริงตามจำนวนนาทีที่กำหนด |
| `src/Web/Composition/ServiceRegistry.cs` | เพิ่ม `ResolveClock()` — อ่าน appSetting `ClockOffsetMinutes` |
| `src/Web/Web.config` | เพิ่ม key `ClockOffsetMinutes` (ค่าเริ่มต้นว่าง) พร้อมคำอธิบายการใช้ |

**กลไกกันเผลอ:** ค่านี้ถูกเมินทั้งหมดเมื่อ `<compilation debug="false">` (คือตอน production)
ต่อให้ลืมลบค่าทิ้งไว้ก็ไม่มีทางเลื่อนเวลาของระบบจริงได้

### สถานะการตรวจ

- ✅ build ผ่านครบทุกโปรเจกต์ (18/08/2026)
- ✅ unit test 257 ตัวผ่านหมด
- ✅ 413 ทำงานจริง — ไฟล์ 12 MB / 10.57 MB ได้ 413 พร้อมข้อความ (เดิมได้ 500)
- ✅ `OffsetClock` ถูกหยิบไปใช้จริง — เลื่อนเวลาแล้ว sendDate เปลี่ยนตาม BR-1

**UAT-01 ปิดแล้ว**

---

## 3. สถานะ UAT รอบ 2

ผลทั้งหมดอยู่ใน [docs/uat/UAT-Checklist.md](docs/uat/UAT-Checklist.md) — **commit แล้ว**

| | จำนวน |
|---|---|
| ✅ ผ่าน | **59 / 61** |
| ➖ ทดสอบไม่ได้ (ต้องใช้มือถือจริง) | **2** |
| ☐ ยังไม่ได้กา | **0** |
| ❌ ข้อบกพร่องที่เปิดอยู่ | **0** |

**ทุกหมวดไล่ครบแล้ว** — หมวด 1, 2, 3, 4, 5, 6, 7 · เหลือแต่ข้อที่ยิง HTTP แทนไม่ได้

2 ข้อที่เป็น ➖ : 8.4 (เปิดกล้องหลัง) · 8.5 (ปฏิทินของเครื่อง)

> **ยังปิด UAT ไม่ได้** — ไฟล์ checklist กำหนดเกณฑ์ปิดไว้เองว่า ➖ ต้องเหลือ **0** ข้อ
> แต่ตอนนี้เหลือแค่ 8.4 กับ 8.5 ที่ต้องมีมือถือจริงเท่านั้น ข้อบกพร่องปิดครบแล้ว

### UAT-01 โดยย่อ (ปิดแล้ว — เก็บไว้เป็นบันทึก)

ไฟล์ที่ใหญ่เกิน **8 MB** (`maxRequestLength`) ถูก ASP.NET ตัดทิ้งตั้งแต่ตอนอ่าน entity body
ซึ่งเกิด **ก่อน** เข้า controller โค้ดตรวจขนาด/ชนิดไฟล์ใน `PhotoService` จึงไม่มีโอกาสทำงาน

| ไฟล์ที่ทดสอบ | ผลก่อนแก้ |
|---|---|
| PDF 1 MB | ✅ "รองรับเฉพาะไฟล์รูปแบบ JPG และ PNG เท่านั้น" |
| PDF 12 MB | ❌ HTTP 500 ไม่มีข้อความ |
| PNG 2.64 MB | ✅ "ไฟล์ใหญ่เกิน 2 MB (2706 KB)" |
| PNG 10.57 MB | ❌ HTTP 500 ไม่มีข้อความ |

ผู้ทดสอบเจอเองจากการลาก **PDF ขนาดใหญ่** ใส่ฟอร์ม จึงตั้งความรุนแรงเป็น **กลาง**

---

## 4. เรื่องที่ต้องตัดสินใจ

### 4.1 สคริปต์ทดสอบรายหมวด 5 ตัว — ✅ ตัดสินใจแล้ว 19/08/2026 : เก็บเข้า `tools\`

เดิมอยู่ในโฟลเดอร์ temp ของ session เก่า เสี่ยงหายทุกเมื่อ · ตอนนี้ย้ายเข้าโปรเจกต์แล้ว
พร้อมแก้ตามเงื่อนไขที่ตั้งไว้เองว่า **ต้องล้างข้อมูลทดสอบที่ตัวเองสร้าง**

| ไฟล์ | ครอบคลุม | assertion |
|---|---|---|
| `tools\Test-Section1.ps1` | หมวด 1 (เข้าสู่ระบบ + สิทธิ์) | 21 |
| `tools\Test-Section2.ps1` | หมวด 2 (แจ้งงาน) | 24 |
| `tools\Test-Section3.ps1` | หมวด 3 (คิวงาน + state machine) | 35 |
| `tools\Test-Section4.ps1` | หมวด 4 (รูป + BR-4) | 26 |
| `tools\Test-Section6.ps1` | หมวด 6 (รายงาน + export) | 27 |

**สิ่งที่แก้ตอนย้ายเข้ามา**

1. **รวมฟังก์ชันซ้ำไว้ที่ `tools\TestSupport.ps1`** — เดิมทั้ง 5 ตัวก๊อป
   `Get-Token` / `New-Session` / `Assert` ไปคนละชุด แก้ตัวหนึ่งอีกตัวก็เพี้ยน
2. **ล้างข้อมูลตัวเองตอนจบ** — จำ `MAX(ReqId)` ก่อนเริ่ม แล้วลบทุกใบที่เกิดหลังจากนั้น
   · ตารางลูกทุกตัวเป็น `ON DELETE CASCADE` อยู่แล้ว ลบใบแม่ใบเดียวพาลูกไปหมด
   · ไฟล์รูปบน filesystem ไม่มีใครลบให้ จึงต้องอ่าน `FilePath` มาลบก่อนลบแถว
   · **ไม่ล้างเมื่อมีข้อไม่ผ่าน** — ข้อมูลตอนพังคือหลักฐาน · สั่ง `-KeepData` เพื่อเก็บไว้เสมอก็ได้
3. **ไฟล์ชั่วคราวย้ายไป temp ของเครื่อง** — เดิมเขียนรูป/ไฟล์ xlsx ลงโฟลเดอร์ตัวเอง
   ถ้าปล่อยไว้จะกลายเป็นขยะใน `tools\`

**ทดสอบแล้ว 19/08/2026 : ผ่านครบ 133/133 assertion** และฐานข้อมูลกลับมาเท่าเดิมเป๊ะ
(61 ใบ · รูป 8 รูป — เท่ากับก่อนรัน) ไฟล์รูปบนดิสก์ก็ตรงกับจำนวนแถวใน DB

> ⚠ อย่ารันตอนมีคนใช้เว็บอยู่ — ใบที่คนอื่นสร้างระหว่างนั้นจะอยู่เหนือเส้นแบ่งแล้วโดนลบไปด้วย
> (ยอมรับได้เพราะเป็นฐาน dev เครื่องเดียว)

### 4.3 ล้างข้อมูลทดสอบใน DB — ✅ ตัดสินใจแล้ว 19/08/2026 : **ยังไม่ล้าง**

ตอนสำรวจจริงพบว่าตัวเลข "27 ใบ" ที่เคยจดไว้**ต่ำกว่าความจริง** — ในฐานมี **61 ใบ
และทั้งหมดเป็นข้อมูลทดสอบ** ไม่มีข้อมูลจริงปนเลย แค่คนละที่มา :

| ที่มา | จำนวน | ตัวอย่างผู้ติดต่อ |
|---|---:|---|
| สคริปต์ทดสอบสร้าง | 44 | `บริษัท ทดสอบ...` · `ปลายทางทดสอบหมวด 4` |
| คนกรอกเองตอนลองระบบ | 17 | `chaiyaporn B` · `111` · `aaaa111` · `2222` |

**ตัดสินใจ : เก็บไว้ทั้งหมด** — ข้อมูลชุดนี้ทำให้หน้าคิวงาน/รายงานมีของแสดงเวลาเปิดดู
ถ้าล้างจะเหลือหน้าว่างเปล่าทุกหน้า · ฐานนี้เป็นฐาน dev ไม่ใช่ฐานที่จะส่งมอบอยู่แล้ว

**สำรองไว้แล้วก่อนตัดสินใจ** (เผื่อเปลี่ยนใจภายหลังจะได้ล้างแบบไม่ต้องกลัว) :

```
C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\Backup\
  MessengerDb-before-cleanup-20260819-2136.bak
```

> ถ้าจะล้างวันหลัง มี 2 ระดับให้เลือก — ลบทั้ง 61 ใบแล้ว reset `tblReqNoSequence`
> ให้เลขใบงานเริ่มที่ 0001 ใหม่ (ได้ฐานสะอาดพร้อมทดสอบรอบใหม่)
> หรือลบเฉพาะ 44 ใบที่สคริปต์สร้าง แล้วเก็บของที่คนกรอกเองไว้ดู

### 4.2 push commit — ✅ เสร็จแล้ว 18/08/2026

push 15 commits ขึ้น `origin/main` แล้ว (`a8bf3c0..0ca058f`)
ตอนนี้ `main` กับ `origin/main` ตรงกัน — งานหลังจากนั้นยังต้องสั่ง push เองทุกครั้ง

---

## 5. สภาพแวดล้อมที่ทิ้งไว้

- **เว็บ** รันอยู่ที่ `http://localhost:52080` (IIS Express, background)
  · ⚠ ตัวที่เปิดค้างจากเมื่อวาน **ไม่ตายเอง** ต้อง `Stop-Process` ก่อนเปิดตัวใหม่ ไม่งั้นตัวใหม่
  ขึ้นไม่ได้ ("Cannot create a file when that file already exists" = พอร์ตถูกจองอยู่)
- **Web.config** อยู่ในสภาพปกติ : `EmailPickupDirectory` = ว่าง · `ClockOffsetMinutes` = ว่าง
- **ข้อมูลทดสอบใน DB** ใบงาน **61 ใบ** · รูป **8 รูป** — ตกลงกันแล้วว่า**เก็บไว้** (§4.3)
  · `tblReqNoSequence` เดินไปถึง SDC 0068 / SBK 0002 แล้ว (เลขที่หายไประหว่างทาง
  คือใบที่สคริปต์สร้างแล้วลบตัวเองทิ้ง — เป็นเรื่องปกติ ไม่ใช่ข้อมูลหาย)
  · มีไฟล์สำรองก่อนหน้านี้เก็บไว้ในโฟลเดอร์ Backup ของ SQL Server
- **พนักงาน `20099`** ถูก cache ลง `tblEmployee`/`tblUserRole` จากการทดสอบ 1.3

---

## 6. กับดักที่เจอวันนี้ (กันพลาดซ้ำ)

1. **เกณฑ์ UAT ที่ผูกกับสภาพข้อมูล ณ วันที่เขียน จะทดสอบซ้ำไม่ได้**
   เจอ 3 ข้อ — 7.4 (`MSG-SBK-...-0001`), 1.3 (`10099` ไม่มีใน DB), 3.2 (ลำดับ 1,2,3)
   · 7.4 กับ 1.3 แก้ถ้อยคำเกณฑ์ให้ทดสอบซ้ำได้แล้ว · 3.2 หลบด้วยการเลือกวันที่คิวยังว่าง

2. **`MockSsoClient` มีรหัส "คนใหม่" แค่ 2 ตัว (`10099`, `20099`) ใช้หมดแล้วทั้งคู่**
   จะทดสอบ 1.3 อีกต้องเพิ่มรหัสในสตับ หรือลบแถวออกจาก DB ก่อน

3. **ClosedXML เขียน XML โดยมี namespace prefix `x:`** (`<x:c>`, `<x:row>`)
   regex ที่ไม่เผื่อไว้จะหาเซลล์ไม่เจอ แล้ว assertion แบบ "ไม่เจอของเสีย" จะ
   **ผ่านแบบเปล่า ๆ** — ตอนตรวจไฟล์ export ต้องยืนยันก่อนเสมอว่าอ่านเจอข้อมูลจริง

4. **การซ่อนปุ่มบนหน้าจอไม่ใช่หลักฐานว่าปลอดภัย** (D33) — ทุกข้อที่เป็น "ไม่มีปุ่ม"
   ต้องยิง POST ตรงไปที่ action ซ้ำเสมอ ทำแบบนี้ครบทุกข้อในรอบนี้แล้ว

5. **`dotnet test` ทำให้ build โปรเจกต์ Web พังชั่วคราว** — restore ของ `dotnet` เขียนทับ
   assets ของ Web จนขึ้น `error : Your project file doesn't list 'win' as a "RuntimeIdentifier"`
   · แก้ด้วยการรัน `msbuild -t:restore` คั่นก่อน build ใหม่ (ไม่ใช่บั๊กของโค้ด)

6. **PowerShell 7 โยน error เมื่อเจอ redirect ทั้งที่ใส่ `-SkipHttpErrorCheck`**
   ถ้าตั้ง `-MaximumRedirection 0` ("maximum redirection count has been exceeded")
   · ในสคริปต์ทดสอบให้ปล่อยตาม redirect ไปเลยแล้วอ่านข้อความ TempData จากหน้าปลายทาง
   — จะได้ทั้งสถานะและข้อความในครั้งเดียว

7. **ข้อ UAT ที่เขียนว่า "จากมือถือ" ไม่ได้แปลว่าต้องมีมือถือเสมอไป** — 4.5 พิสูจน์เรื่อง
   การย่อรูปใน `<canvas>` ซึ่งเป็นพฤติกรรมของ**เบราว์เซอร์** มือถือเป็นแค่แหล่งที่มาของไฟล์ใหญ่
   · แทนด้วยไฟล์ 4032×3024 บนคอมได้ครบ แล้วสั่ง Chrome headless ผ่าน DevTools Protocol
   ให้กดแทนคน (`tools\photo-resize-probe.mjs` — Node 22+ มี `WebSocket` ในตัว จึง**ไม่ต้องเพิ่ม
   dependency** อย่าง puppeteer/playwright)
   · เวลาวัด "ไฟล์ที่ส่งจริง" ให้เชื่อ **Content-Length จาก Network event ของเบราว์เซอร์**
   เป็นหลัก ส่วนค่าที่อ่านจากตัวหน้าเว็บใช้เป็นตัวเทียบว่าไฟล์ถูกแปลงเป็นอะไร

8. **รูปทดสอบต้องเป็นไฟล์ที่ถอดรหัสเป็นภาพได้จริง** ไฟล์ขยะที่แค่มี magic bytes ถูก
   จะตกที่ `loadImage()` แล้วดูเหมือนข้อบกพร่องทั้งที่โค้ดถูก · และอย่าใช้ noise ล้วน
   เพราะบีบอัดไม่ลง ทำให้ผลที่ควรผ่านกลับไม่ผ่าน — ใช้ลายไล่เฉด + noise เม็ดเล็กแทน

9. **Bootstrap ตั้ง `scroll-behavior:smooth` ไว้ที่ `:root`** (อยู่ใน reboot ของ 5.3)
   ทำให้ `scrollIntoView()` เป็นแอนิเมชัน — สคริปต์ที่วัดกรอบทันทีหลังเลื่อนจะได้ค่าเก่า
   แล้ว `elementFromPoint` คืน `null` ให้ทุกอย่างที่อยู่นอกจอ ดูเหมือน "ปุ่มกดไม่โดนทั้งหน้า"
   ทั้งที่หน้าเว็บปกติดี · ต้องสั่ง `behavior:'instant'` เสมอเวลาวัด layout

10. **ปุ่มที่ `disabled` ไม่รับ pointer event** — `elementFromPoint` จะคืน element แม่ให้แทน
    เป็นพฤติกรรมที่ถูกต้อง (ปุ่มปิดใช้งานก็ไม่ควรกดได้) การตรวจ "กดโดนไหม" จึงต้อง
    ยกเว้นปุ่มพวกนี้ ไม่งั้นแถวแรก/แถวสุดท้ายของคิวจะฟ้องทุกครั้งเพราะปุ่ม ↑ ↓ ถูกปิดไว้

11. **สคริปต์ทดสอบที่ไม่ล้างข้อมูลตัวเอง = ระเบิดเวลา** — หลัง UAT รอบ 2 ฐาน dev
    บวมขึ้นเป็น 61 ใบโดยที่เกือบครึ่งเป็นขยะจากเทสต์ · วิธีที่ใช้แก้คือจำ `MAX(ReqId)`
    ก่อนเริ่มแล้วลบทุกอย่างที่เกิดหลังจากนั้น (ดู `tools\TestSupport.ps1`)
    · แต่ **อย่าล้างตอนเทสต์ไม่ผ่าน** ไม่งั้นหลักฐานที่ต้องใช้หาสาเหตุจะหายไปพร้อมกัน
